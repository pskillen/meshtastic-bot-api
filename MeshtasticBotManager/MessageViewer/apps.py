"""Django app configuration for the MessageViewer application."""

from django.apps import AppConfig


class MessageviewerConfig(AppConfig):
    """Configuration class for the MessageViewer Django app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "MessageViewer"
