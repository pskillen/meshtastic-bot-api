{# Navbar #}
<nav class="navbar bg-base-100 shadow-md">
    <div class="container mx-auto flex justify-between items-center">

        {# Logo & Brand Name #}
        <a href="{{ url('home') }}" class="text-xl font-bold text-primary">
            Meshtastic Bot Manager
        </a>

        {# Navbar Links - Desktop #}
        <div class="hidden md:flex space-x-4">
            <a href="{{ url('home') }}" class="btn btn-ghost">Home</a>
            <a href="{{ url('channel') }}" class="btn btn-ghost">Messages</a>
             <a href="{{ url('nodes') }}" class="btn btn-ghost">Nodes</a>
            <a href="{{ url('admin:index') }}" class="btn btn-primary">Admin</a>
        </div>

        {# Mobile Menu Button #}
        <div class="md:hidden">
            <button class="btn btn-square btn-ghost" onclick="toggleMenu()">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24"
                     stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"/>
                </svg>
            </button>
        </div>
    </div>
</nav>

{# Mobile Menu #}
<div id="mobile-menu" class="hidden bg-base-100 p-4 shadow-md">
    <a href="{{ url('home') }}" class="block py-2">Home</a>
    <a href="{{ url('channel') }}" class="block py-2">Messages</a>
     <a href="{{ url('nodes') }}" class="block py-2">Nodes</a>
    <a href="{{ url('admin:index') }}" class="block py-2 text-primary font-bold">Admin</a>
</div>

{# Mobile Menu Toggle Script #}
<script>
    function toggleMenu() {
        let menu = document.getElementById('mobile-menu');
        menu.classList.toggle('hidden');
    }
</script>
