"""Application configuration for the PacketLogging app."""

from django.apps import AppConfig


class PacketLoggingConfig(AppConfig):
    """Configuration class for the PacketLogging application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "PacketLogging"
